<form method="post" action="{{ path('app_bonbon_delete', {'id': bonbon.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bonbon.id) }}">
    <button class="btn">Delete</button>
</form>
